{
  "name": "atomic-logic-mcp",
  "version": "3.0.0",
  "description": "MCP server for building logically rigorous arguments using atomic reasoning",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsc --watch",
    "prepare": "npm run build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "validate-system-r": "npm run build && npm run test",
    "system-r-compliance": "npm run build && npm run test test/system-r-complete.test.ts"
  },
  "keywords": ["mcp", "server", "logic", "planning", "evidence", "arguments"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
